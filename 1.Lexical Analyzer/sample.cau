
fn add(a: int, b: int) int {
	let c:int = a + b;
	return c;
}

fn main() {
	let a:int = input();
	let b:int = input();
	let c:int = add(a,b);

	jump(L,(c != 12));
	print("Equal!");
	jump(M, true);
	L: {
		print("Not equal");
	}
	M: {
			
	}
	return 0;
}

END
